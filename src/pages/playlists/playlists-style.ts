import styled from "styled-components"

export const PlaylistsWrapper = styled.div`
  margin-top: 38px;
  .playlists {
    display: flex;
    flex-wrap: wrap;
    gap: 18px;
  }

  .pagination {
    display: flex;
    justify-content: center;
    margin-top: 48px;
  }
`
