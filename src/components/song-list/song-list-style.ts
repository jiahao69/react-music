import styled from "styled-components"

export const SongListWrapper = styled.div`
  .even-row {
    background-color: #fafafa;
  }

  .operate-btns {
    display: flex;
    gap: 16px;

    .iconfont {
      height: 21px;
      color: #666;
      font-size: 20px;
      cursor: pointer;
    }
  }
`
