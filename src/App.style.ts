import { styled } from "styled-components"

export const AppWrapper = styled.div`
  .app-wrapper {
    padding: 0 256px;

    .nav-bar-wrapper {
      padding: 16px 0;
      padding-left: 206px;
    }
  }
`
